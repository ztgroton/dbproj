
gen_test_colnames <- function(n = 10, min = 2, max = 20) {
  
  # Validate Inputs 
  if (missing(n)) {stop("`n` is missing in call to `gen_test_colnames`")}
  if (missing(min)) {stop("`min` is missing in call to `gen_test_colnames`")}
  if (missing(max)) {stop("`max` is missing in call to `gen_test_colnames`")}
  
  
  
}
